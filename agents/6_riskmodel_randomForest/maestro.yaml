name: riskModel
description: This agent scores shipments by combining a rule-based baseline model and a trained ML model, selecting the higher delay probability to assign the final risk level.
entrypoint: main.py
runtime: uvicorn
dockerfile: dockerfile
version: 0.1.0

requirements:
  - fastapi
  - uvicorn
  - requests
